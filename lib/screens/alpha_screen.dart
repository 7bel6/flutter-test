import 'package:flutter/material.dart';

class AlphaScreen extends StatelessWidget {
  const AlphaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("AlphaScreen"),
      ),
    );
  }
}
